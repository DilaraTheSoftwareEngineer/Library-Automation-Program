<dx:ThemedWindow x:Class="LibraryAutomationn.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
    WindowStyle="None" AllowsTransparency="True" Background="Transparent"
    Title="Login" Height="450" Width="400" WindowStartupLocation="CenterScreen">

    <Grid>
        
        <Border CornerRadius="20" Background="#0F051D"/>
        <Border Margin="15" CornerRadius="25" Background="#12FFFFFF" BorderBrush="#25FFFFFF" BorderThickness="1.5">
            <StackPanel VerticalAlignment="Center" Margin="30">
                <TextBlock Text="LOGIN" Foreground="White" FontSize="32" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,30"/>

                <TextBlock Text="Kullanıcı Adı" Foreground="#AAFFFFFF" Margin="0,10,0,5"/>
                <TextBox x:Name="txtUser" Background="#15FFFFFF" Foreground="White" BorderThickness="1" Height="35"/>

                <TextBlock Text="Şifre" Foreground="#AAFFFFFF" Margin="0,15,0,5"/>
                <PasswordBox x:Name="txtPass" Background="#15FFFFFF" Foreground="White" BorderThickness="1" Height="35"/>

                <Button Content="Giriş Yap" Margin="0,30,0,0" Height="40" Background="#FF00E5FF" Foreground="#0F051D" FontWeight="Bold" Click="Login_Click"/>
                <Button Content="Kapat" Margin="0,10,0,0" Background="Transparent" BorderThickness="0" Foreground="Gray" Click="Close_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</dx:ThemedWindow>